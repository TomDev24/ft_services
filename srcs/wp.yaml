apiVersion: v1
kind: Service
metadata:
  name: wordpress
  labels:
    app: wordpress
  annotations:
    metallb.universe.tf/allow-shared-ip: ekvm
spec:
  ports:
    - port: 5050
      targetPort: 5050
  selector:
    app: wordpress
    tier: frontend
  type: LoadBalancer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wordpress
  labels:
    app: wordpress
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wordpress
      tier: frontend
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: wordpress
        tier: frontend
    spec:
      containers:
      - image: wordpress
        imagePullPolicy: Never
        name: wordpress
        #env:
        #- name: WORDPRESS_DB_HOST
        #  value: wordpress-mysql
        #- name: WORDPRESS_DB_PASSWORD
        #  valueFrom:
        #    secretKeyRef:
        #      name: mysql-pass
        #      key: password
        ports:
        - containerPort: 5050
          name: wordpress
          # volumeMounts:
          #- name: wordpress-persistent-storage
          # mountPath: /www/localhost
          # volumes:
          # - name: wordpress-persistent-storage
          # persistentVolumeClaim:
          # claimName: wp-pv-claim
